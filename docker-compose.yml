services:
  mongodb:
    image: mongo:latest
    container_name: ezbiz_mongodb
    restart: unless-stopped
    ports:
      - "127.0.0.1:27018:27017"
    volumes:
      - mongodb_data:/data/db

volumes:
  mongodb_data: